<section id="advantages" class="benefits" aria-labelledby="advantages-title">
  <div class="container benefits__grid">
    <div class="benefits__intro">
      <header class="section-heading">
        <h2 id="advantages-title">Почему выбирают нас</h2>
        <span class="section-index">/02</span>
      </header>

      <p class="benefits__lead">
        Профессиональный подход, понятные условия и аккуратная работа на участках разной сложности.
      </p>
    </div>

    <article class="benefits__tile benefits__tile--acid">
      <h3>Комплексный подход</h3>
      <p>Оцениваем участок, подбираем технику и способ работ под фактическое состояние территории.</p>
    </article>

    <article class="benefits__tile benefits__tile--dark">
      <h3>Сроки и без задержек</h3>
      <p>Согласовываем задачу и время выезда до начала работ.</p>
    </article>

    <article class="benefits__tile benefits__tile--dark">
      <h3>Честные условия</h3>
      <p>Стоимость зависит от площади, сложности и фактического состояния участка.</p>
    </article>
  </div>
</section>

<style>
  .benefits {
    background: var(--color-paper);
  }

  .benefits__grid {
    display: grid;
    grid-template-columns: 1.15fr 1fr 1fr 1fr;
    border-right: 1px solid var(--color-line);
    border-bottom: 1px solid var(--color-line);
    border-left: 1px solid var(--color-line);
  }

  .benefits__intro {
    min-width: 0;
    padding: clamp(1.4rem, 2vw, 1.9rem);
  }

  .benefits__intro h2 {
    max-width: 12ch;
  }

  .benefits__lead {
    max-width: 34ch;
    margin-top: 1.35rem;
    color: var(--color-text-muted);
    font-size: 0.76rem;
    line-height: 1.55;
  }

  .benefits__tile {
    display: flex;
    min-height: 300px;
    flex-direction: column;
    justify-content: flex-end;
    padding: clamp(1.35rem, 2vw, 1.8rem);
    border-left: 1px solid var(--color-line);
  }

  .benefits__tile--acid {
    background: var(--color-acid);
    color: var(--color-ink);
  }

  .benefits__tile--dark {
    border-left-color: var(--color-line-dark);
    background: var(--color-ink);
    color: var(--color-paper);
  }

  .benefits__tile--dark h3 {
    color: var(--color-paper);
  }

  .benefits__tile h3 {
    font-size: clamp(1.35rem, 1.7vw, 1.9rem);
  }

  .benefits__tile p {
    margin-top: 0.75rem;
    font-size: 0.74rem;
    line-height: 1.5;
    opacity: 0.8;
  }

  @media (max-width: 1000px) {
    .benefits__grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .benefits__intro {
      grid-column: 1 / -1;
    }

    .benefits__tile:nth-of-type(1),
    .benefits__tile:nth-of-type(3) {
      border-left: 0;
    }

    .benefits__tile {
      border-top: 1px solid var(--color-line);
    }
  }

  @media (max-width: 620px) {
    .benefits__grid {
      width: 100%;
      grid-template-columns: 1fr;
      border-right: 0;
      border-left: 0;
    }

    .benefits__tile {
      min-height: 230px;
      border-left: 0;
    }
  }
</style>
